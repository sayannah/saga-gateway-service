spring:
  application:
    name: saga-gateway-service
  data:
    redis:
      host: redis
      port: 6379

server:
  port: 6001
security:
  signature-verification:
    enabled: false # enable for verification
