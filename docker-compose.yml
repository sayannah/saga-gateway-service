services:
  redis:
    image: redis:7
    container_name: redis
    ports:
      - "6379:6379"

  external-ledger:
    build:
      context: ../external.ledger
      dockerfile: Dockerfile
    container_name: external-ledger
    ports:
      - "6002:6002"

  gateway:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "6001:6001"
    depends_on:
      redis:
        condition: service_started
      external-ledger:
        condition: service_started
    environment:
      SERVER_PORT: 6001
      SPRING_REDIS_HOST: redis
      SPRING_REDIS_PORT: 6379
      SYSTEM_B_WS_URL: ws://external-ledger:6002/mock-ws
      SIGNATURE_VERIFICATION_ENABLED: "false"
      SIGNATURE_PUBLIC_KEY_PATH: "classpath:keys/public.pem"
      systema.webhook.url: "http://host.docker.internal:8081/api/v1/ack"
    deploy:
      replicas: 1